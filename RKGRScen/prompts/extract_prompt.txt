SYSTEM:
你是自动驾驶测试场景结构化提取器。你必须严格输出 JSON，不要输出任何解释。你的任务是把交通事故叙事提取为结构化逻辑场景 L = (V_type, A, R_req, E_cond)，其中 V_type 是目标违规类型，A 是参与者集合，R_req 是粗粒度道路需求，E_cond 是天气与时间条件。请保留叙事中的因果与时间顺序关系。

USER:
事故叙事:
{narrative}
可选违规类型: {allowed_violation_types}
请输出严格 JSON，包含以下顶层字段：
- violation_type：目标违规类型（只能从可选违规类型中选择）
- likelihood：违规可能性评分（1-5 的整数）
- reason：判断依据（描述违规如何发生、涉及哪些因果与时间顺序）
- law：法规依据
- dsl：结构化描述，必须包含 road_network（type/lanes）、actors（model/initial_position/actions/speed_limit）和 env（time/weather）
